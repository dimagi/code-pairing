name: "Test workflow"
on:
  pull_request:
    branches:
    - master

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Read state.txt file
        id: getdidrun
        run: echo "{name}={value}" >> $GITHUB_OUTPUT
      - name: Run
        if: {{ $GITHUB_OUTPUT }} == "did_run"
        run: echo "Success"
